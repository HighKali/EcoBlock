#!/usr/bin/env python3\nimport platform, os\ndef eco_status():\n print("🧠 EcoAgent Diagnostica")\n print(f"OS: {platform.system()} {platform.release()}")\n print(f"User: {os.environ.get("USER", "unknown")}")\n print("Moduli chiave:")\n for f in ["ecoentry.sh", "ecoheal_ui_fix.py", "ecoapi.py"]:\n  print(f" - {f}: {"✅" if os.path.exists(f) else "❌"}")\neco_status()
